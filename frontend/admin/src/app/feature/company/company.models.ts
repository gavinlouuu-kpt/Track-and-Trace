export interface ICompanies {
  status: string;
  limit: number;
  offset: number;
  searchString: string;
  selectedSupplyChain: string;
  selectedCountry: string;
  sortBy: string;
  orderBy: string;
}
